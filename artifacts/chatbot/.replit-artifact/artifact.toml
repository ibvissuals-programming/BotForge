kind = "web"
previewPath = "/"
title = "Business Chatbot"
version = "1.0.0"
id = "artifacts/chatbot"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/chatbot run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/chatbot", "run", "build" ]
publicDir = "artifacts/chatbot/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
