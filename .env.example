# BotForge — required environment variables
# On Replit: add these in the Secrets tab (sidebar → Secrets), NOT in a .env file.
# On a local clone: copy this file to .env and fill in real values.

# Groq API key — get yours at https://console.groq.com
GROQ_API_KEY=
